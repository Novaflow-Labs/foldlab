# Normalize line endings: store text as LF in the repo and check out LF
# everywhere (keeps Windows/macOS/Linux consistent, silences CRLF warnings).
* text=auto eol=lf

# Never normalize these binary-ish assets.
*.png binary
*.jpg binary
*.ico binary
